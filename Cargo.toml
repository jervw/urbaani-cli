[package]
name = "urbaani"
version = "0.6.1"
edition = "2021"

[dependencies]
reqwest = {version = "0.11", features = ["blocking"]}
scraper = "0.13.0"
termion = "1.5.6"
textwrap = "0.15.0"

# binary size optimizations
[profile.release]
strip = true
opt-level = "s"
lto = true
codegen-units = 1
